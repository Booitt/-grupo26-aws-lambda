[
	{
		/* Automático */
		"id": "string",
		/* Obrigatório */
		"nome": "string",
		/* Obrigatório */
		"email": "string",
		/* Opcional */
		"telefone": "string",
		/* Automático, criado no momento da requisição */
		"prospectSince": "date",
		/* Ao criar a lead, essa propriedade é inicializada com o valor false, 
		para facilitar um filtro posteriormente. No momento em que 
		o cliente faz uma compra pelo mesmo e-mail cadastrado na lead, 
		essa propriedade é atualizada com a data do momento da compra. */
		"clientSince": "date | false"
	}
]
